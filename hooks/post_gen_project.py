import subprocess

solution_name = '{{cookiecutter.solution_name}}'

# subprocess.check_call([f'dotnet new sln -o {solution_name}'])

# api_name = '{{cookiecutter.api_name}}'

# subprocess.check_call([f'dotnet sln add ./{api_name}/{api_name}.csproj'], cwd=solution_name)
# subprocess.check_call([f'dotnet sln add ./{api_name}.Tests/{api_name}.Tests.csproj'], cwd=solution_name)
